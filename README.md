# MacTranscribe — landing & download site

Static single-page site for MacTranscribe. Deploy anywhere static (GitHub Pages,
Cloudflare Pages) or via the cluster (Argo). The download button tracks the latest
GitHub Release of the app.
